var React = require('react');
var Nav = require('./Nav');

var PageLayout = React.createClass({
    render: function()
    {
        return(
            <Nav />
        )
    }

});

module.exports = PageLayout;
